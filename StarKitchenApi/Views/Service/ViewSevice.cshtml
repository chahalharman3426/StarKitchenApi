@model IEnumerable<StarKitchenApi.Models.ServicesCredential>

@{
    ViewBag.Title = "ViewSevice";
}

<h2>View Sevice</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Specification)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Charges)
        </th>
        <th>Modify</th>
        <th>Remove</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Specification)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Charges)
        </td>
        <td>
            <p class="btn btn-warning">@Html.ActionLink("Edit", "Edit", new { id = item.id }) |</p></td>
        <td>
            <p class="btn btn-warning">
                @Html.ActionLink("Delete", "Delete", new { id = item.id })</p>
        </td>
    </tr>
}

</table>

<a href="@Url.Action("AdminZone","Admin")">Back to Dashboard</a>